import { Text, View } from "react-native";
import Navbar from "../navigation/topNavbar/navbar";

function Index() {
	return (
		<View>
			<Navbar title="Settings" rightIcons={[]} />
		</View>
	);
}

export default Index;
